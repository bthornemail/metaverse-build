#!/usr/bin/env bash
# Capability: Sync
# Authority: automata-metaverse
# Justification: ../../runtime/sync/adapters/typescript/automata-metaverse/ADAPTER-JUSTIFICATION.md
# Inputs: validated trace payload (stdin)
# Outputs: projected sync payload (stdout)
# Trace: no
# Halt-On-Violation: yes

set -euo pipefail

# Projection-only adapter runner (no semantics, no authority)
# Placeholder until real adapter invocation is wired.
cat
